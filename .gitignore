.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
android/.gradle/
android/local.properties
ios/Pods/
ios/.symlinks/
flutter_app/pubspec.lock

# Release signing — must never be committed. Created fresh in CI from
# GitHub Secrets right before a release build, discarded after.
android/key.properties
android/app/release-key.jks
*.jks
